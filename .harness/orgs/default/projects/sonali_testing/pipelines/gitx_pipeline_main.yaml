pipeline:
  stages:
    - name: Deploy Kubernetes_ba0b
      id: Deploy_Kubernetes_ba0b
      service:
        type: kubernetes
        items: <+input>
      environment:
        items: <+input>
      runtime: delegate
      on-failure:
        - errors:
            - all
          action: stage-rollback
      steps:
        - name: step1
          run:
            script: echo 'hello'
          id: step1
