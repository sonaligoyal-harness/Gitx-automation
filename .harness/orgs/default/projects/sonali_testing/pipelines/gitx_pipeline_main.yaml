pipeline:
  stages:
    - name: Deploy Kubernetes_ba0b
      id: Deploy_Kubernetes_ba0b
      service:
        type: kubernetes
        items:
          - id: service_kubernetes_2a02
            with:
              overlay:
                type: Kubernetes
                spec:
                  artifacts:
                    primary:
                      primaryArtifactRef: <+input>
                      sources: <+input>
            ref: 19Apr
      environment:
        items: <+input>
      runtime: delegate
      on-failure:
        - errors:
            - all
          action: stage-rollback
      steps:
        - name: step1
          run:
            script: echo 'hello'
          id: step1
