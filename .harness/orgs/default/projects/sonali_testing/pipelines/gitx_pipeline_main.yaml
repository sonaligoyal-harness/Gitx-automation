pipeline:
  stages:
    - name: Deploy Kubernetes_ba0b
      id: Deploy_Kubernetes_ba0b
      service:
        type: kubernetes
        items:
          - id: gitx_service_main
            with:
              overlay:
                type: Kubernetes
                spec:
                  manifests:
                    - manifest:
                        identifier: m1
                        type: K8sManifest
                        spec:
                          store:
                            type: CustomRemote
                            spec:
                              extractionScript: <+input>
      environment:
        items: <+input>
      runtime: delegate
      on-failure:
        - errors:
            - all
          action: stage-rollback
      steps:
        - name: step1
          run:
            script: echo 'hello'
          id: step1
